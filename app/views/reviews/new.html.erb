<div class="container">
  <h1> <%= @dessin.name %> - Review</h1>

  <%= simple_form_for [@dessin, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit "Submit review", class: "btn btn-primary" %>
  <% end %>
</div>
